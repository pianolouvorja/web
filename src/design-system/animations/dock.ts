/** Motion tokens — dock style macOS */
export const dockAnimation = {
  hoverScale: 1.25,
  hoverLift: '-4px',
  duration: '300ms',
  easing: 'cubic-bezier(0.22, 1, 0.36, 1)',
} as const

export const themeTransition = {
  duration: '280ms',
  easing: 'ease',
} as const
